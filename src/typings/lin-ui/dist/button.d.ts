import { ComponentType } from 'react';
declare const LButton: ComponentType<{
  bindlintap?: any;   // 按钮在非禁用状态下点击所触发的事件
  loading?: boolean;  // 是否为加载中按钮
  disabled?: boolean; // 按钮是否禁用
}>;
export default LButton;